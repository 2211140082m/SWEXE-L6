<% total_price = 0 %>
<% @cartitems.each do |e| %>
    商品名：<%= Product.find(e.product_id).name %>
    価格：<%= Product.find(e.product_id).price %>
    数量：<%= e.qty %>
    <%= link_to "削除", cart_item_path(e), data: { turbo_method: :delete } %>
    <br>
    <% total_price += Product.find(e.product_id).price * e.qty %>
<% end %>
合計金額：<%= total_price %>円
<br>

<%= link_to "トップに戻る", root_path%>